# API Configuration
NEXT_PUBLIC_API_URL=http://localhost:8000

# Note: Location autocomplete (country, state, city) uses local data
# No API key required - all suggestions are loaded from static lists

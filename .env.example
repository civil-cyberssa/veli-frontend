# NextAuth Configuration
# Secret usado para encriptar tokens JWT (gere um valor aleatório seguro)
# Pode ser gerado com: openssl rand -base64 32
NEXTAUTH_SECRET=your-secret-here-change-this-in-production

# URL da aplicação (usado pelo NextAuth)
NEXTAUTH_URL=http://localhost:3000

# API Backend Configuration
# URL base da API do backend
NEXT_PUBLIC_API_URL=https://api.jogajuntoinstituto.org
